// Entry point for Firebase Cloud Functions.
// Individual function implementations live in aiCamera.ts, textToProfile.ts,
// and usageQuota.ts — left as stubs for now, per Section 1.2 of the system
// design doc (AI calls proxied through Cloud Functions to keep API keys and
// usage-quota enforcement off the client).

export {};
